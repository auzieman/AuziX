{
  "format": "auzix-command-suite-v1",
  "name": "OSTree",
  "version": "2025.6",
  "description": "Content-addressed filesystem store used by Flatpak runtimes and applications.",
  "source": {"type": "host-binary", "suite": "lab", "package": "ostree", "graduation": "upstream-source"},
  "depends": ["BusyBox"],
  "commands": [{"name": "ostree", "host_command": "ostree"}],
  "validation": {"smoke_commands": ["$AUZIX_COMMAND_ROOT/ostree.real --version 2>&1 | grep -i ostree"]}
}
