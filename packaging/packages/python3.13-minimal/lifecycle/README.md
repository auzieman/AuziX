AUZiX installs the versioned Python runtime and command links as package-owned
payload. Debian dpkg queries, runtime hook dispatch, binfmt registration, and
/usr/local policy are donor protocol and are not executed.
