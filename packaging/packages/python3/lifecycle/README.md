Python3 is a metapackage. AUZiX publishes Python entry points and runtime paths
from native packages, so Debian alternatives, documentation migration, and
maintainer-script protocol are not retained.
