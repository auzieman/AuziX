{
  "fixture": "blas-lapack-provider-review",
  "package": "Libblas3+Liblapack3",
  "contract": {"model_output_is_review_only": true, "apk_has_no_update_alternatives_contract": true},
  "evidence": {
    "install": ["update-alternatives publishes libblas.so.3 or liblapack.so.3 from a package-owned nested provider at priority 10"],
    "remove": ["remove the same provider unless this is an upgrade"],
    "migration": ["pre-multiarch provider removal guarded by dpkg version comparison"],
    "trigger": ["activate-noawait ldconfig"],
    "boundary": [
      "AUZiX publishes libraries through /Libraries and must retain ownership-safe provider selection",
      "the real sources are package payload files under the BLAS and LAPACK nested library directories",
      "APK payload ownership naturally removes the selected public link; no upgrade detector or unregister database is required",
      "do not call dpkg, update-alternatives, or ldconfig",
      "do not invent /System/Settings/alternatives, register_provider, or payload rewrite targets"
    ]
  },
  "question": "Propose native AUZiX provider publication and removal semantics shared by BLAS and LAPACK. Separate current portable intent from obsolete Debian migration protocol.",
  "_oracle": {
    "minimum_operations": 2,
    "minimum_payload_rewrites": 0,
    "maximum_payload_rewrites": 0,
    "required_terms": ["provider", "publish", "ownership", "blas", "lapack", "payload"],
    "required_discard_terms": ["dpkg", "update-alternatives", "ldconfig", "upgrade"],
    "forbidden_terms": ["systemctl", "rm -rf", "/System/Settings/alternatives", "register_provider", "unregister", "upgrade detection"],
    "exclusive_concepts": [{"terms": ["update-alternatives"]}],
    "unresolved_must_not_be_scripted_terms": ["upgrade", "/usr/local", "conflict"]
  }
}
