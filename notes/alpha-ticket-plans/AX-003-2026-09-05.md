AX-003 / task56 — September 5, 2026 — scoped intake/packaging plan

Terminal data and launcher delivery

Evidence and limits: Confirmed: intake already adds terminfo paths to command wrappers. Shared launcher fix ba246ae adds relocated NcursesBase; repaired TerminologyData archive is pinned. Two different launch paths must agree.

Existing owners: scripts/build-auzix-debian-intake-package.sh; scripts/stage-auzix-terminal-launcher.sh; scripts/build-auzix-desktop-integration-package.sh.

Next bounded action: Compare retained donor data paths/triggers and generated command wrappers against emitted APKs. Verify menu/dock/rescue terminal Exec reaches the existing wrapper with interactive shell and terminfo paths. Repair publication only if the installed output differs; do not rebuild ncurses blindly.

Acceptance: Fresh installed image resolves xterm-256color and packaged colorscheme under normal auzix environment. Terminology, rescue terminal and xterm launch usable shells; htop/Glances render correctly after AX-002.

Execution/rollback: prepare and commit any repair, then execute through BKC with
retained commands, logs, exit status and artifact hashes. Preserve reference
VM145 and prior candidate. Revert only the identified producer/input change;
no direct SSH repair. This is a plan, not a completed fix. Common reconciliation:
notes/alpha-scoped-reconciliation-2026-09-05.md.

