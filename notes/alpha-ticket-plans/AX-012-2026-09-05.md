AX-012 / task65 — September 5, 2026 — scoped intake/packaging plan

Operator correction (15:56 PDT): the 35 "failed" packages are not an AuziX
OS/runtime defect. Debian installs them (dbus=1.16.2-2 observed). BKC
prove-factory exit 1 is the intake/APK mapper leaving effects unmapped
(`lifecycle_intake.py`, debian-intake builder, FPM hooks). Do not rebuild
D-Bus, recompile donors, or "fix AuziX" from a needs-review row.

Operator correction (15:58 PDT): do not write 35 special adapters. The factory
already extracts Debian control/scripts (`dpkg-deb -e`) and path-rewrites
payloads. The stuck point is `_apply_generated_script_rules` / leftover
`UNSUPPORTED_PATTERNS` treating Debian protocol (`dpkg-statoverride`,
`adduser --system`, `DPKG_ROOT`, generated init/systemd) as unmapped
package bugs. Re-extract that script logic generically and inject AuziX
paths. Families, not package names.

Scoped installation-effect accounting

Evidence and limits: Confirmed: intake retains donor pre/post scripts, triggers/conffiles/config/templates. Mapping/FPM exists. PACKAGE-PASS is archive verification; static means no detected lifecycle scripts. The current review identified overlapping publication and independent image overrides.

Existing owners: scripts/build-auzix-debian-intake-package.sh; auzix/lifecycle_intake.py; auzix/archive_fpm.py; auzix/fpm.py; selected release profiles and BKC pipeline.

Next bounded action: For only these ticket packages, inventory donor effects as preserved/adapted/inapplicable-with-reason/unresolved, link effect to emitted hook/owned path. Add negative and replay tests for mappings actually changed. Separate conversion/install/VM/human acceptance labels. Keep exception provenance; do not expand into all distro packages or redesign FPM.

Acceptance: One reproducible selected-input-to-installed-output receipt per changed node, plus failure propagation. Compare a prepared Trixie install transaction through BKC where retained metadata cannot answer an effect. All repairs prepared+committed then BKC-executed; direct SSH discovery only.

HDD lock (16:04 PDT): VM145 is the close-enough reference. Do not assemble
another HDD until prove-factory writes `validation-boundary.json` and the
container lane (zero → nginx → netinstall/pre-hdd) has a passing receipt.
Remaining `needs-review` rows are intake mapping, not an HDD trigger.

Planning correction (16:15 PDT): `95000cc` failed in 0.4s because lifecycle
applied the payload `/usr*` sed map to unowned script paths. Next change is
layer-split only (owned RootFS, runtime leftovers including `/var/run`,
payload Compatibility stays in debian-intake). Local `unittest discover`
must be green before the next BKC prove-factory. See
`notes/alpha-intake-validate-plan-2026-09-05.md`.

Unpack walk (16:30 PDT): four smallest r2 holds vs Debian `postinst`.
The 35 are leftover protocol tokens after a partial import
(`in_sysroot`/`DPKG_ROOT`, `dpkg --compare-versions`, `invoke-rc.d`,
`sysctl`). Next family work is those four shapes. See
`notes/alpha-held-five-unpack-2026-09-05.md`.

Template (16:50 PDT): leftover `triggers` / generated enable are one
family. Import as `--apk-trigger` + `/Services/<name>/run`, not more
`auzix_*` helpers. See `notes/alpha-service-trigger-template-2026-09-05.md`.

17:02 PDT: leftover `dpkg-helper` must not fail intake. Import the
donor protocol as AuziX operations. See
`notes/alpha-dpkg-helper-not-auzix-2026-09-05.md`.

17:12 PDT: wrap those needed steps as `auzix_needed_step` and re-measure
the held set (prove-factory r6 vs r5). HDD still locked.

17:16 PDT: r6 leftovers are where/when, not missing items. Shared layout
plus PATH / first-boot. See `notes/alpha-leftover-where-when-2026-09-05.md`.

17:39 PDT: Debian's maintainer script is dpkg's product. apk has its own
db. Do not haul Dpkg/Debconf so that script can run. Adapted = apk
scriptlet / trigger / owned path. See
`notes/alpha-apk-owns-the-db-2026-09-05.md`.

17:42 PDT: BML r7 — convert or strip. Measure held set vs r6.
`notes/alpha-intake-validate-r7-run-2026-09-05.md`. HDD still locked.

17:46 PDT: leftover `/bin/systemctl` is Programs/Systemd's command, not a
missing foreign manager. Convert `../bin/systemctl` to
`/System/Compatibility/bin/systemctl`. See
`notes/alpha-systemctl-is-systemd-2026-09-05.md`.

17:50 PDT: leftover stray logic is a Package/legacy artifact, not a hold
and not an apk hook. See `notes/alpha-park-legacy-leftovers-2026-09-05.md`.

17:56 PDT: operator asked to run held r8, then the r10 HDD 117 list.
Held run `20260905-intake-validate-r8` passed (BKC `1f5356d5`,
holds 17→0). HDD list run `20260905-intake-validate-hdd-r8` passed
(BKC `b915d9f4`, 97+20, 0 regressions). 18:10 PDT operator unlocked
HDD after leftover capture. See
`notes/alpha-hdd-unlock-2026-09-05.md`.

18:34 PDT: unlock-r11 pre-hdd passed. HDD BKC `a4e49aa4` to VM 146
failed immediately: host `cp` through exported `/etc/passwd` absolute
symlink. See `notes/alpha-hdd-phase2-fail-passwd-2026-09-05.md`.

20:22 PDT: operator confirmed prior HDD images already had sshd, passwd,
and users through E. VM145 and staged r20 have regular
`/etc/{passwd,group,shadow,gshadow}`. unlock-r11 still has leaf links.
Stager change recorded in
`notes/alpha-hdd-passwd-materialize-2026-09-05.md`.

20:24 PDT: operator asked to retry. Same hdd_id after parking the
failed a4e49aa4 dirs, because the pipeline looks up pre-hdd by the
`alpha-apk-` suffix. See `notes/alpha-hdd-r12-retry-2026-09-05.md`.

20:42 PDT: r12 staging/passwd PASS; validator died on Sudo/current.
Publish current -> host. See `notes/alpha-hdd-sudo-current-2026-09-05.md`.

20:47 PDT: `cce7464a` not hung; assert followed builder-absolute current.
See `notes/alpha-hdd-sudo-current-host-assert-2026-09-05.md`.

Execution/rollback: prepare and commit any repair, then execute through BKC with
retained commands, logs, exit status and artifact hashes. Preserve reference
VM145 and prior candidate. Revert only the identified producer/input change;
no direct SSH repair. This is a plan, not a completed fix. Common reconciliation:
notes/alpha-scoped-reconciliation-2026-09-05.md.

