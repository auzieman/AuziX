AX-002 / task55 — September 5, 2026 — scoped intake/packaging plan

PTY and device activation

Evidence and limits: Confirmed: VM145 live /dev/tty and /dev/pts/ptmx are0666 after repair. ServiceRuntime already mounts devpts with ptmxmode=666 and chmods the devices. Source of the former0660 reset is not established.

Existing owners: scripts/build-auzix-service-runtime-package.sh; boot StartSequence/udev rules; scripts/stage-auzix-alpha-hdd-root.sh.

Next bounded action: Capture relevant Trixie udev rules and VM132 mount/device state, then compare delivered AuziX rules and startup order. Trace whether ServiceRuntime activation runs and whether later device setup replaces nodes. Correct only the identified publication/order omission; no competing chmod loop.

Acceptance: As auzix, create/use a PTY after boot and again after reboot; check owner/mode, shell input, resize and colors. Package presence or a root-run terminal is insufficient.

Execution/rollback: prepare and commit any repair, then execute through BKC with
retained commands, logs, exit status and artifact hashes. Preserve reference
VM145 and prior candidate. Revert only the identified producer/input change;
no direct SSH repair. This is a plan, not a completed fix. Common reconciliation:
notes/alpha-scoped-reconciliation-2026-09-05.md.

