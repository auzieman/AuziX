AX-006 / task59 — September 5, 2026 — scoped intake/packaging plan

Font configuration and cache publication

Evidence and limits: Confirmed: observed missing default fontconfig warning remains unexplained. The fontconfig generation at intake lines746+ is inside the LibreOffice wrapper; it is not proof of global configuration. Do not mark AX-006 fixed from Office behavior.

Existing owners: scripts/build-auzix-debian-intake-package.sh; retained fontconfig/fontconfig-config/font packages and maintainer surfaces; lifecycle_intake.py.

Next bounded action: Inspect donor conffiles, conf.d symlinks and cache-trigger scripts for selected font packages. Compare package-owned fonts.conf and include targets through AuziX paths, global environment and cache permissions with VM132/145. Fix actual missing publication or translated trigger; preserve working app-private configuration.

Acceptance: Normal-user fc-match resolves installed fonts without default-config warning; E icons/text and Office/browser text render correctly. Replay does not overwrite administrator configuration.

Execution/rollback: prepare and commit any repair, then execute through BKC with
retained commands, logs, exit status and artifact hashes. Preserve reference
VM145 and prior candidate. Revert only the identified producer/input change;
no direct SSH repair. This is a plan, not a completed fix. Common reconciliation:
notes/alpha-scoped-reconciliation-2026-09-05.md.

