AX-007 / task60 — September 5, 2026 — scoped intake/packaging plan

Account tools, sudo and image users

Evidence and limits: Confirmed: repaired Passwd archive is pinned; Sudo producer has /Libraries fix. WorkstationUserPolicy currently creates auzix, groups and subuid/subgid inside a package hook, contrary to agreed image ownership. Docker /etc leaf aliases affect Shadow writes.

Existing owners: packaging/packages/passwd/lifecycle/after-install; scripts/build-auzix-sudo-package.sh; scripts/build-auzix-pre-hdd-support-packages.sh; docker/release/common/runtime-entrypoint.sh.

Next bounded action: Compare retained Trixie account/PAM scripts and configuration with emitted generic packages. Separate deployment provisioning from generic package effects only through the existing image setup, preserving working order and sudo policy. Avoid combining that move with unrelated PAM rewrites; first capture equivalent inputs/outputs. Keep Docker-specific limitation explicit.

Acceptance: Provision user/groups before testing. Login as auzix, sudo id/ls/apk, create/delete a disposable user with supported tools, verify homes/modes and SSH. Repeat on actual HDD; do not substitute --user for a login session.

Execution/rollback: prepare and commit any repair, then execute through BKC with
retained commands, logs, exit status and artifact hashes. Preserve reference
VM145 and prior candidate. Revert only the identified producer/input change;
no direct SSH repair. This is a plan, not a completed fix. Common reconciliation:
notes/alpha-scoped-reconciliation-2026-09-05.md.

