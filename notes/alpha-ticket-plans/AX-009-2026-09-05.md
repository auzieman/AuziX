AX-009 / task62 — September 5, 2026 — scoped intake/packaging plan

Flatpak application and export delivery

Evidence and limits: Confirmed: RuntimeSupport repairs /var aliases and configures Flathub when proc is available. That is not application installation. No Firefox installation step was found in the inspected pre-HDD/HDD/user-default scripts; broader pipeline capture still needed.

Existing owners: scripts/build-auzix-flatpak-runtime-support-package.sh; Flatpak donor scripts/triggers; existing application export/menu integration.

Next bounded action: Compare donor setup/cache effects and reference installed GNOME Calculator/exports. Locate existing pipeline app-install provisioner before adding one. Pin agreed Firefox app/ref and retain Calculator through image provisioning; APK owns tools/integration, not user-specific installs. Keep Zed optional and outside this pass.

Acceptance: Actual trusted install, export visible in E menu, usable application launch and reboot persistence. Verify /var compatibility for Bubblewrap. Do not count remote presence as success.

Execution/rollback: prepare and commit any repair, then execute through BKC with
retained commands, logs, exit status and artifact hashes. Preserve reference
VM145 and prior candidate. Revert only the identified producer/input change;
no direct SSH repair. This is a plan, not a completed fix. Common reconciliation:
notes/alpha-scoped-reconciliation-2026-09-05.md.

