AX-010 / task63 — September 5, 2026 — scoped intake/packaging plan

Podman and zero/one payloads

Evidence and limits: Confirmed: Podman definition reuses proven archives and checks only --version. Build pipeline produces zero/one Docker images on R730; that alone does not put them in the HDD Podman store. User policy provides subordinate IDs.

Existing owners: packaging/packages/podman/package.json and adjacent container packages; ServiceRuntime; build-auzix-pre-hdd-apk-image.sh.

Next bounded action: Compare Trixie podman/conmon/crun/containers-common install effects and selected config/helper files. Verify newuidmap/newgidmap, subids, storage/network configuration and cgroup/device access against VM132/145. Locate an existing zero/one import or pull provisioner; supply pinned references only if missing, without touching the live service deployment.

Acceptance: As intended user on HDD: podman info/ps and run zero and one with shell/HTTP checks. Record exact images/digests. Container construction on build server is not this acceptance.

Execution/rollback: prepare and commit any repair, then execute through BKC with
retained commands, logs, exit status and artifact hashes. Preserve reference
VM145 and prior candidate. Revert only the identified producer/input change;
no direct SSH repair. This is a plan, not a completed fix. Common reconciliation:
notes/alpha-scoped-reconciliation-2026-09-05.md.

