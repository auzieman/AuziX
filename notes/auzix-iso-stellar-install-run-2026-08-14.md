# AUZiX guarded ISO/install run — 2026-08-14

## Intent

Base anchor:

- Installer-spine anchor: `auzix-netinstall-express-r4-20260810T024318Z.iso`
  from `notes/express-container-host-and-lean-iso-2026-08-09.md`.
- Graphical package identity anchor:
  `artifacts/auzix/auzix-live-theme-app-candidate.iso` from
  `notes/auzix-known-good-live-desktop-vs-published-repo-2026-08-10.md`.

Minimal delta:

1. Preserve the r4 shell/network/SSH/install behavior.
2. Add ext4/E2fsprogs live preflight.
3. Stage `/System/Tools/auzix-existing-installer-preflight`.
4. Refuse silent ext2 fallback unless `AUZIX_ALLOW_EXT2_FALLBACK=1` is set.
5. Keep desktop/workstation hydration separate from the tiny installer spine.

Packages/profiles touched:

- `packages/tiny-netinstall-remote.profile.json`
- `profiles/packages/auzix-tiny-netinstall-remote.packages`
- `packages/netinstall-reconstruction.profile.json`
- `packages/netinstall-chroot-package-transaction.contract.md`
- `scripts/add-auzix-live-tools.sh`
- `scripts/validate-auzix-live-agent.sh`
- `scripts/auzix-existing-installer-preflight.sh`

Explicit non-goals:

- No broad package rebuild.
- No replacing known-good desktop package identities by friendly-name repo drift.
- No LightDM/E repair loop during tiny installer ISO validation.
- No desktop package hydration until the installed disk boot and SSH gates pass.

Rollback artifact:

- Use r4 installer-spine behavior as rollback model.
- Use `auzix-live-theme-app-candidate.iso` and its documented package identity
  list as the graphical baseline.

## Pre-build gates

- [x] Confirm current source has the guarded installer contract.
  - `scripts/add-auzix-live-tools.sh` syntax check passed.
  - `scripts/auzix-existing-installer-preflight.sh` syntax check passed.
- [x] Confirm live-tools staging copies `auzix-existing-installer-preflight`.
  - `add-auzix-live-tools.sh` copies it to
    `/System/Tools/auzix-existing-installer-preflight`.
- [x] Confirm validation fails if the preflight guard is absent.
  - `validate-auzix-live-agent.sh` now requires the staged executable.
- [x] Confirm normal installer path refuses silent ext2 fallback.
  - `auzix-install-disk` generated by `add-auzix-live-tools.sh` exits unless
    ext4 tooling exists or `AUZIX_ALLOW_EXT2_FALLBACK=1` is explicitly set.
- [x] Confirm tiny installer profile excludes desktop payloads and native
      `Coreutils`.
  - `packages/tiny-netinstall-remote.profile.json` and
    `profiles/packages/auzix-tiny-netinstall-remote.packages` exclude native
    `Coreutils`; BusyBox remains the tiny installer control-plane surface.
- [ ] Confirm chosen build lane is thin assembler or explicit tiny installer
      lane, not broad workstation/package rebuild.

Seam noted before build:

- `scripts/add-auzix-live-tools.sh` still contains desktop helper/starter logic.
  For the tiny installer spine, build/profile flags must keep GUI autostart out
  of the acceptance path. Presence of E helper code is not permission to hydrate
  or repair the desktop during installer-spine validation.

## Outcome

ISO artifact:

SHA-256:

VM target:

Booted BIOS/UEFI:

DHCP:

SSH:

`auzix-installer validate`:

ext4 preflight:

disk install:

ISO detached:

disk boot:

LightDM:

keyboard:

mouse:

Enlightenment:

terminal:

menus:

selected app launches:

logs/screenshots:

verdict:

## 2026-08-14 BKC safe pipeline result

BKC run:

- `e55a4b0d-68a0-42e4-9a6c-78341face20d`
- Pipeline: `auzix-package-repo-stripped-iso`
- Gates intentionally left disabled: package publish, stripped ISO build, VM135 install validation.

Outcome:

- Package intent validation passed.
- Repository build from receipts completed.
- Flatpak runtime slice built.
- Strict-root audit receipt was captured, but the audit still reports hard failures.
- Ollama review ran, but its suggested VM/cloud workflow language is generic and not authoritative for AUZiX lab operations.

Do not enable ISO or VM135 gates yet.

Current blockers from receipts:

- `Strace` package build reports missing host command `strace`.
- `NetSurf` package build reports missing host command `file`.
- Strict-root audit still reports legacy top-level compatibility links under invalid policy.

Resolved in the focused follow-up:

- `artifacts/auzix/repo/index.json` permissions were corrected on the OpenStack master share.
- `scripts/test-auzix-installer.sh` now runs packaged Lua through the packaged loader instead of leaking host loader/library assumptions.
- The installer test harness now uses packaged `jq` through the packaged loader instead of requiring host `jq`.
- The stale graphical `execute` test was replaced with the current installer `run` contract plus destructive-confirmation checks.
- `scripts/audit-auzix-strict-root.sh` now runs packaged `jq` through the packaged loader for JSON contract checks.

Latest focused audit:

- `out/package-repo-stripped-iso/strict-root-audit-rerun-2.txt`
- Installer contract checks: PASS.
- Installer guarded execution tests: PASS.
- Runtime identity/network/browser baseline: PASS.
- Remaining audit failures: 11 expected strict-policy failures for classic top-level compatibility links when `AUZIX_LEGACY_POLICY=invalid`.
- Current alpha compatibility audit: PASS.
- Compatibility audit receipt: `out/package-repo-stripped-iso/strict-root-audit-compat-rerun.txt`.
- Meaning: the staged root is internally consistent with declared AUZiX compatibility aliases; true zero-classic-link work remains a later strict-root migration target, not tonight's alpha installer gate.

BKC follow-up:

- BKC executor/defaults were adjusted so the safe alpha proof run uses `AUZIX_LEGACY_POLICY=compat`.
- Fresh visible run queued: `6bd0bf04-7bb9-46db-ab4a-8afc7b96d239`.
- Heavy gates remain disabled: no package publish, no ISO build, no VM135 install validation.
- Early receipt still shows known builder host gaps: `strace` and `file`.

Builder host-tool fix:

- `BlackKnightController/Dockerfile` now installs `file` and `strace` so BKC worker-local package stages have the same tiny host-tool floor the package source catalog expects.
- Running OpenStack BKC containers were hot-patched with `file` and `strace` for immediate verification.
- `docker/trixie-builder/Dockerfile` now installs `strace`; it already carried `file`.
- Clean proof rerun queued: `4d87d0ca-6222-4184-b004-2583ede55381`.
- Early clean-rerun evidence: `Strace` built successfully and `NetSurf` advanced past the old missing-`file` gate into the actual NetSurf package script.

Next smallest safe phase:

1. Decide whether this run should use `AUZIX_LEGACY_POLICY=compat` for the current alpha line or truly remove the classic top-level links before strict-invalid ISO work.
2. Add missing builder host tools only if they are truly required for this safe run (`strace`, `file`).
3. Re-run the same safe BKC pipeline.
4. Only after those gates pass, consider enabling ISO validation/build gates.

## 2026-08-14 compact ISO build receipt

Reason for direct R730 build:

- BKC visible pipeline currently has a static executor stage plan for
  `auzix-package-repo-stripped-iso`; the optional ISO gate in the pipeline JSON
  is not yet wired into that executor path.
- The first R730 wrapper attempt rebuilt the full builder image and stalled
  before the ISO stage during Docker/apt work. The current `auzix/builder:lab`
  image already had the tools needed for ISO assembly, so rebuilding it was not
  required for this pass.
- Direct ISO attempts using NFS-backed source/work directories hit slow/stuck
  cleanup and tar-copy behavior. The successful pass used local R730 source,
  root, and ISO work directories.

Failed/stopped attempts retained as receipts:

- `alpha-compat-iso-20260814T182113Z`
  - stopped before ISO stage
  - reason: builder image rebuild stalled before the direct ISO stage
- `direct-alpha-compat-iso-20260814T182322Z`
  - stopped
  - reason: NFS-backed `AUZIX_ISO_WORK_DIR` cleanup stalled at
    `rm -rf /workspace/out/auzix-iso`
- `direct-localwork-alpha-compat-iso-20260814T182444Z`
  - failed immediately
  - reason: `AUZIX_ISO_WORK_DIR` pointed at the bind mount root `/iso-work`
    and the builder correctly refused to remove it
- `direct-localwork2-alpha-compat-iso-20260814T182513Z`
  - stopped
  - reason: NFS source-root tar entered uninterruptible IO wait during root
    payload copy

Successful ISO:

- run id: `direct-localroot-alpha-compat-iso-20260814T182859Z`
- ISO name:
  `auzix-alpha-compat-direct-localroot-alpha-compat-iso-20260814T182859Z.iso`
- local R730 artifact:
  `/var/lib/auzix-build/runs/alpha-compat-iso-20260814T182113Z/src/artifacts/auzix/auzix-alpha-compat-direct-localroot-alpha-compat-iso-20260814T182859Z.iso`
- published NS1 artifact:
  `/mnt/ns1/AuziX/src/artifacts/auzix/auzix-alpha-compat-direct-localroot-alpha-compat-iso-20260814T182859Z.iso`
- size: `393M`
- SHA-256:
  `6f43cbb053d316cb07bfa9ba5b9e5a39d7df83c19446fe72598f70629c9634fb`
- build log:
  `/mnt/ns1/AuziX/build-receipts/live-build-direct-localroot-alpha-compat-iso-20260814T182859Z.log`
- validation log:
  `/mnt/ns1/AuziX/build-receipts/live-build-direct-localroot-alpha-compat-iso-20260814T182859Z.validate.log`
- receipt:
  `/mnt/ns1/AuziX/build-receipts/live-build-direct-localroot-alpha-compat-iso-20260814T182859Z.receipt`

Validation:

- `validate-auzix-boot-iso.sh` passed with `AUZIX_REQUIRE_UEFI=1`.
- `xorriso` reported BIOS and UEFI El Torito boot images.
- Volume ID: `AUZIXLIVE`.

Guardrail for next run:

- Keep ISO source, root snapshot, and ISO workdir on R730 local disk.
- Publish the finished ISO to NS1 only after validation and size check.
- Do not mutate VMID135 until the ISO artifact exists, validates, and has a
  receipt.
