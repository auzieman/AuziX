September 5, 2026 15:58 PDT — AX-012/task65.

The 35 holds do not each need special care. Factory already extracts Debian
scripts; the mapper must re-play that logic with AuziX paths. Next change is
generic protocol families in lifecycle_intake (statoverride add, system
account), not a D-Bus adapter. After that proof, repackage the HDD list
through intake so overcorrected Debian logic is imported again. No HDD or
VM145 change in this step.
